封装了两个api方法，一个是open，一个是close

在调用open的时候传入一些参数，比如窗口的宽度，加载的内容地址url等

就是通过定位的方式，根据传入的窗口的宽度参数，然后用浏览器的宽度减去窗口的宽度除以2，这是它定位的left地址


position:fixed

不要重复说话

没明白？

假设第一个是最大的，然后从第2个开始遍历，和第一个比较，如果有比它大的，交换

升序排一下，最后一个就是最大的

Math.max()

apply

知道了

Math.max.apply(null, [22,2,2,2,])

默认按字母

arr.sort(function(a, b){
	return a - b;
})


可以，这时候传入的a和b就是对象

arr.sort(function(a, b){
	return a.key - b.key;
})

访问一个对象的属性时，先查找自己的构造函数中是否有，没有找


假设有一个btn

就是当外部函数


作用域链，当内部函数在执行时，会在作用域链中查找之前引用的变量，而这个变量因为

react/angular

做过练习

首先和angular比要简单很多，学习成本比较低，是用来开发组件的，使用mvc中的view，但是有的时候感觉

没听明白

在底层内存中构建一个虚拟的DOM结构，在DOM改变时先在内存中修改，然后一次性渲染到真实的dom树中

应该有一个和dom树的对比


首先一定不能一更改就进行dom操作

我后有一个类似观察者模式的这样的一个功能，观察着dom结构的变化，

我听说过一个叫Fragment文档碎片的这么一个api，它能解决这个问题吗

是的，这个比较容易想到

就是将真实的dom操作减少，通过使用定时器来定时操作

是的，所以需要类似观察者模式的这样来监测

需要在内存中构造一个数据结构来将用户的改变进行记录，然后对比，有改动再修改dom结构

不要重复说话

我先要整体预览一下，看看这个设计稿的大至布局，
看看有哪些重用的部分，需要定义在公共的样式文件中

还要看设计师的要求，是否需要响应式，根据要求，进行设计
再看对浏览器的兼容性要求，也要适当调整布局的设计，这些都想好以后再开始编码

@media (max-width: 480px){
	
}
???

右面的不浮动

想一想,等一会

/<\w+[^>]/



